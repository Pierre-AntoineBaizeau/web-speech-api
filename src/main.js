import { initializeRecognition } from "./speechInit.js";
import { processCommand, commands } from "./commandProcessor.js";
import { feedbackVoice } from "./voiceFeedback.js";

export { initializeRecognition, processCommand, commands, feedbackVoice };

// Use these functions in this main.js similar to how you used them in index.html
